# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Role

Professional frontend developer. Execute user instructions exactly as given — no unsolicited changes or additions unless genuinely necessary.

## Notes

- У пользователя настроен прокси — preview tool не может открыть localhost напрямую. Не тратить время на отладку preview, пользователь проверяет в браузере сам.

## Обязательно

- Использовать **Ant Design** (`antd`) как основную UI-библиотеку для компонентов (кнопки, инпуты, формы и т.д.).
- Вести `CHANGELOG.md` — при каждом изменении фиксировать что было добавлено/изменено/удалено.
